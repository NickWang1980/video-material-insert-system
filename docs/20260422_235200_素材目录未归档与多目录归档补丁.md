# 2026-04-22 23:52:00 素材目录未归档与多目录归档补丁

## 变更目标
- 增加“未归档”目录可见性与模板可选范围。
- 支持素材在目录树中拖拽归档到多个脚本子文件夹。
- 将目录关系落到数据库属性，支持增删查与任务匹配精确查找。

## 本次补丁
- 后端：
  - `material_service` 在“产品分类素材”查找时改为按 `material_folder_bindings` 多对多关系匹配。
  - 增加“未归档”范围解析（模板 `素材库分类=未归档`）。
  - `csv_utils` 扩展允许值：`定量素材/未归档/产品分类素材`。
- 前端：
  - `ConfigEditor` 的 `素材库分类` 下拉新增“未归档”。
  - 素材文件名联动筛选新增“未归档”分支。
- 文档：
  - 更新 `README.md` 素材库能力描述（含未归档与拖拽多归档）。
  - 更新 `docs/API接口文档.md` 素材接口定义为当前实现口径。

## 兼容性
- 旧模板不含 `素材库分类` 时默认仍为“定量素材”。
- 历史任务与素材记录保持可读；多目录归档能力在现有结构上向后兼容。

任务完成
