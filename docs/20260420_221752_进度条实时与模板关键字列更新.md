# 进度条实时与模板关键字列更新

- 时间：2026-04-20 22:17:52
- 目标：实现“进度条实时显示+未完成呼吸效果”，并在“配置模板列表”新增关键字列。

## 变更摘要
- `frontend/src/components/common/ProgressBar.vue`
  - 增加 `status` 入参。
  - 未完成任务（`pending/processing`）显示呼吸动效。
  - 未完成且进度接近 0 时保留最小可见宽度。
- `frontend/src/components/task/TaskTable.vue`
  - 传入 `row.status` 给进度条，驱动动效状态。
- `frontend/src/views/Home.vue`
  - 新增轮询刷新（3 秒）任务与统计，运行中任务自动实时更新。
- `frontend/src/views/TaskList.vue`
  - 新增轮询刷新（3 秒）任务列表，运行中任务自动实时更新。
- `frontend/src/views/TaskDetail.vue`
  - 新增轮询刷新（2 秒）任务进度与日志（运行中时）。
- `backend/app/api/configs.py`
  - `GET /api/config-templates` 返回 `keywords` 与 `keyword_preview`。
- `backend/app/schemas/config.py`
  - 扩展 `ConfigTemplateResponse`：`keywords`、`keyword_preview`。
- `frontend/src/views/ConfigList.vue`
  - 新增“关键字”列，展示 `keyword_preview`。

## 兼容性
- 现有模板结构与编辑器无需迁移。
- 旧模板列表接口调用方不受影响（新增字段为增强信息）。
