# 2026-04-24 混剪单元页面实施记录

## 变更摘要
- 新增独立页面 `混剪单元`，入口路径：`/rough-cut/unit`
- 保留现有 `多角色混剪`，两套页面并存
- 复用 `rough-cut` 后端域，补齐项目更新、动态角色、自动预览调度和列表摘要字段

## 本次实现
- 后端
  - `POST /api/rough-cut/projects` 支持 `scriptFileName`
  - 新增 `PATCH /api/rough-cut/projects/{id}`
  - 项目详情与列表响应补充 `scriptFileName / roleCount / assetCount / stageSummary`
  - 剧本支持按角色前缀动态生成角色卡
  - 角色素材 ASR 全部达标后自动触发预览版混剪
- 前端
  - 新增 `RoughCutUnit.vue`
  - 项目列表与 CRUD
  - 剧本 TXT 上传与替换
  - 动态角色素材卡、自动上传、实时 ASR 进度
  - 分镜头列表、时间线、预览播放器、ASR 匹配对比中心

## 读写范围
- 读取
  - `backend/app/api/rough_cut.py`
  - `backend/app/services/rough_cut_service.py`
  - `backend/app/schemas/rough_cut.py`
  - `frontend/src/views/RoughCutMultiRole.vue`
  - `frontend/src/api/roughCut.js`
  - `frontend/src/router/index.js`
- 写入
  - `backend/app/models/database.py`
  - `frontend/src/views/RoughCutUnit.vue`
  - `README.md`
  - `docs/API接口文档.md`
  - `docs/部署指南.md`

## 兼容说明
- 旧页面 `多角色混剪` 不删除、不替换
- 旧接口路径保留
- 生产环境需要重新构建前端，后端需要重启后才可从 `8000` 访问新页面
