# 任务字幕烧录改为可选并默认关闭
- 时间：2026-04-21 17:04:36
- 需求：创建任务时提供复选框“添加字幕SRT到视频”，默认不添加；输出视频默认不烧录字幕。

## 后端变更
1. 新增任务字段 dd_subtitle_to_video（默认 alse）
   - ackend/app/models/task.py
   - ackend/app/models/database.py 兼容迁移：旧库自动补列。
2. 创建任务接口新增表单参数
   - ackend/app/api/tasks.py：POST /api/tasks 支持 dd_subtitle_to_video。
   - 任务响应增加 dd_subtitle_to_video。
3. 合成逻辑按任务开关决定是否烧录字幕
   - ackend/app/services/task_service.py：创建任务保存该开关，处理任务时传入视频服务。
   - ackend/app/services/video_service.py：仅在开关为 	rue 时添加 subtitles filter。

## 前端变更
1. 新建任务弹窗新增复选框
   - rontend/src/components/task/CreateTaskModal.vue
   - 文案：添加字幕SRT到视频
   - 默认值：未勾选。
2. 创建任务请求增加字段
   - rontend/src/api/task.js
   - 新增 dd_subtitle_to_video 到 FormData。

## 文档同步
- README.md：补充“任务级字幕烧录开关，默认关闭”。
- docs/API接口文档.md：补充 dd_subtitle_to_video 请求与响应字段。

## 说明
- 本轮未代跑测试。
- 若使用 8000 生产态，请先构建前端后再以 prod 启动。
- 任务完成
