# 源视频库 + 首页流程图 + 任务列表增强实施记录

- 时间：2026-04-21 10:51:59
- 范围：后端任务创建链路、源视频库 API、首页流程图、任务列表/详情交互、文档同步

## 本轮已完成

1. 后端能力
   - 新增 `source_video_entries` 数据模型并接入初始化。
   - 任务模型新增 `source_entry_id` 兼容字段。
   - 新增 `/api/source-videos`（创建/列表/重命名/删除）。
   - `POST /api/tasks` 改为 `task_name + source_entry_id + config_ids[]`。
   - 新增 `POST /api/tasks/{id}/stop`，支持立即停止并转 `stopped`。
   - 修复任务删除逻辑：源视频条目任务不再删除源视频/SRT文件。

2. 前端页面与交互
   - 新增“源视频库”页面（创建条目、重命名、删除）。
   - 新建任务弹窗改造为“源视频条目 + 多模板”。
   - 首页重建并加入流程图（上传素材 → 模板 → 源视频条目 → 任务 → 下载）。
   - 任务列表支持行内：下载成品/下载报告/下载日志/停止。
   - 任务详情补充停止按钮、进度百分比、源视频来源展示。
   - 进度条未完成态呼吸效果保留并提升对比度。

3. 文档更新
   - 更新 `README.md`。
   - 更新 `docs/API接口文档.md`。
   - 更新 `docs/部署指南.md`。

## 兼容性说明

- 历史任务仍可读取；`source_video` 不存在时返回空。
- 任务快照中的模板来源继续保留，便于追溯。
- 删除源视频条目时，如存在关联任务会返回 `400` 拒绝删除。

## 本轮未执行

- 未执行自动化测试与端到端回归（按当前协作约束，代码已完成并待你本地验证）。
