# 回滚到 Docker 改造前（基线 2026-04-21 15:21）
- 时间：2026-04-21 16:46:13
- 目标：回滚代码与文档到 Docker 改造前口径；保留 `data/`。

## 本次回滚内容
1. 删除 Docker 工程文件：`docker-compose.yml`、`.dockerignore`、`backend/Dockerfile`。
2. 回滚 `scripts/start_all.sh`：仅保留本地开发与 `--prod`，移除 Docker 相关模式与检测逻辑。
3. 回滚 `backend/.env.example`：删除 Docker 示例配置段。
4. 回滚主文档：`README.md`、`docs/部署指南.md`，移除 Docker 启动说明。
5. 删除 Docker 专项变更记录文档（6 份）。

## 保留项
- `data/` 目录及其中数据库、素材与产物全部保留，不做清理。

## 说明
- 本轮未代跑测试（按约定）。
- 生效方式：重新执行启动脚本即可。
- 任务完成
