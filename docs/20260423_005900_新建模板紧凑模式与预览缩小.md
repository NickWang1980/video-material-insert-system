# 2026-04-23 00:59:00 新建模板紧凑模式与预览缩小

## 变更内容
- 新建/编辑模板表格改为紧凑布局，字段按“素材绑定/时间参数/显示参数”分组，减少列宽占用。
- 在不调整字体大小的前提下，降低横向宽度压力，默认可在常见桌面分辨率下查看整表，不依赖水平滚动。
- 素材预览缩略图缩小到原来的约 25%（由 `24x24` 调整为 `6x6`）。
- 增加紧凑表格样式：单元格上下内边距收窄、输入控件宽度自适应列宽。

## 影响文件
- `frontend/src/views/ConfigEditor.vue`

任务完成
