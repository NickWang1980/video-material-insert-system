# 变更记录：素材库视图/侧栏折叠/任务播放

时间：2026-04-22 16:25:04

## 本次改动

1. 素材库视图调整（按需求）
- 移除“当前方式”视图。
- 仅保留两种视图：`预览`、`详细`。
- 默认视图改为 `预览`。
- 文件：`frontend/src/views/MaterialLibrary.vue`

2. 左侧导航支持收起/展开
- 侧栏新增折叠开关按钮（`«/»`）。
- 折叠时显示窄栏与单字入口，展开时显示完整名称。
- 折叠状态写入 `localStorage`，刷新后保持。
- 文件：
  - `frontend/src/components/layout/Sidebar.vue`
  - `frontend/src/App.vue`

3. 任务列表新增“播放”按钮
- 每条任务新增行内 `播放` 按钮。
- 有成品文件时可直接打开播放（新标签页）；无成品时禁用并提示。
- 文件：`frontend/src/components/task/TaskTable.vue`

## 兼容性
- 仅改前端展示与交互，不改后端 API。
- 现有下载/重试/停止/删除逻辑保持不变。
