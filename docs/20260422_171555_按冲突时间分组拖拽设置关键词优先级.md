# 变更记录：按冲突时间分组拖拽设置关键词优先级

时间：2026-04-22 17:15:55

## 本次改动
- 新建任务“模板层级设置”改造为：
  - 仅显示与关键词冲突相关的模板行；
  - 按冲突时间段分组展示；
  - 组内支持拖拽上下调整关键词优先顺序。
- 任务提交新增冲突优先级参数 `collision_priority_json`。
- 后端在同一字幕行命中冲突时，优先按该组拖拽顺序选 winner；未配置则回退原规则。

## 影响文件
- `frontend/src/components/task/CreateTaskModal.vue`
- `frontend/src/api/task.js`
- `backend/app/api/tasks.py`
- `backend/app/services/task_service.py`
- `backend/app/services/material_service.py`
- `backend/app/models/task_snapshot.py`
- `backend/app/models/database.py`
- `docs/API接口文档.md`

## 兼容性
- 不传 `collision_priority_json` 时，行为与现有逻辑兼容。
- 仍保留“同名关键词跨模板冲突阻止创建”规则。
