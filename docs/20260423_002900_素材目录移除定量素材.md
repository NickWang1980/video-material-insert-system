# 2026-04-23 00:29:00 素材目录移除“定量素材”

## 变更目标
- 在素材目录中移除“定量素材”节点，仅保留“未归档”和“产品分类素材”。

## 具体实现
- 前端素材库目录树删除“定量素材”节点，默认进入“未归档”。
- 拖拽归档与上传入口不再提供“定量素材”目标，仅支持“未归档/脚本子文件夹”。
- 旧 `library_kind=general` 兼容处理：
  - API 入参 `general` 自动映射为 `unfiled`；
  - 列表按 `unfiled` 查询时会带出历史 `general` 数据；
  - 响应展示统一按 `unfiled` 返回。
- 启动迁移将历史 `materials.library_kind='general'` 归一到 `unfiled`。

## 文档同步
- 更新 `README.md`、`docs/API接口文档.md` 的素材目录描述。

任务完成
