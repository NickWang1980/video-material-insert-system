# 2026-04-23 多角色混剪工作台实施记录

## 变更时间
- 2026-04-23

## 本次目标
- 在现有应用中新增“多角色混剪”独立模块，不影响现有任务/模板/素材主流程。

## 后端变更
- 新增路由：`backend/app/api/rough_cut.py`
  - `GET/POST /api/rough-cut/projects`
  - `GET /api/rough-cut/projects/{id}`
  - `POST /api/rough-cut/projects/{id}/assets`
  - `POST /api/rough-cut/projects/{id}/split-script`
  - `POST /api/rough-cut/projects/{id}/assign-roles`
  - `PUT /api/rough-cut/projects/{id}/settings`
  - `POST /api/rough-cut/projects/{id}/build-timeline`
  - `PATCH /api/rough-cut/projects/{id}/sentences/{sentence_id}`
  - `POST /api/rough-cut/projects/{id}/regenerate-sentence/{sentence_id}`
  - `POST /api/rough-cut/projects/{id}/export`
  - `GET /api/rough-cut/projects/{id}/status`
  - `GET /api/rough-cut/projects/{id}/media`
- `backend/main.py` 挂载混剪路由。
- `backend/app/services/rough_cut_service.py` 增加 `update_project_settings`。
- `backend/app/schemas/rough_cut.py` 增加 `UpdateSettingsRequest`。

## 前端变更
- 新增页面：`frontend/src/views/RoughCutMultiRole.vue`
  - 三栏工作台（文案/句子、预览/时间线、规则/参数/素材/日志）。
  - 一键混剪、重新生成、导出MP4。
  - 单句保存、单句重生、单句预览。
  - 角色素材按A/B/C分组上传。
  - 进度条 + 轮询状态更新。
- 新增 API 客户端：`frontend/src/api/roughCut.js`。
- 新增路由：`/rough-cut/multi-role`。
- 侧边栏新增“多角色混剪”入口。
- Header 标题映射新增“多角色混剪”。

## 文档更新
- `README.md`：补充混剪模块说明与CLI启动方式。
- `docs/API接口文档.md`：补充混剪接口清单。
- `docs/部署指南.md`：补充混剪页面访问与流程说明。

## 编译与启动 CLI
1. `pip install -r backend/requirements.txt`
2. `cd frontend && npm install`
3. `cd frontend && npm run build`
4. `python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`

## 备注
- 本次仅实现混剪主路径（P0），未引入复杂特效与外部云服务。
