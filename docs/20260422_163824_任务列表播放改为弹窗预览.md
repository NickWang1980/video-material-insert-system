# 变更记录：任务列表播放改为弹窗预览

时间：2026-04-22 16:38:24

## 变更内容
- 将任务列表中的“播放”行为从新标签页打开改为弹窗内播放。
- 弹窗样式与素材库视频预览保持一致：`el-dialog + <video controls preload="metadata">`。
- 关闭弹窗后销毁播放器（`destroy-on-close`），避免继续占用资源。
- “下载成品/下载报告/下载日志”保持原有行为与入口。

## 影响文件
- `frontend/src/components/task/TaskTable.vue`

## 兼容性说明
- 不涉及后端接口变更。
- 继续使用 `GET /api/tasks/{id}/output` 作为播放源。
