# 8000 端口生产态最新功能可见性修复
- 时间：2026-04-21 17:00:44
- 目标：确保通过 8000 端口（生产态）访问时，始终加载最新前端功能。

## 代码调整
1. scripts/start_backend.sh
   - 新增 --prod 模式。
   - --prod 下用无 --reload 的 uvicorn 启动；默认仍是开发态 --reload。
2. scripts/start_backend.bat
   - 新增 --prod 模式，与 .sh 行为对齐。
3. scripts/start_all.sh
   - --prod 分支改为：前端 build 后调用 ./scripts/start_backend.sh --prod。
4. scripts/start_all.bat
   - --prod 分支改为：前端 build 后调用 scripts\start_backend.bat --prod。

## 文档同步
- README.md：补充开发启动与生产启动命令区分。
- docs/部署指南.md：补充单独后端 --prod 启动说明。

## 说明
- 本轮未代跑测试。
- 若要让 8000 看到最新功能，需先构建前端再以 --prod 启动后端。
- 任务完成
