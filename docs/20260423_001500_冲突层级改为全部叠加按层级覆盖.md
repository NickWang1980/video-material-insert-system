# 2026-04-23 00:15:00 冲突层级改造（全部叠加 + 按层级覆盖）

## 目标
- 同一字幕行关键词冲突时，不再抑制低优先级素材。
- 冲突组内素材全部参与叠加，并按“冲突层级设置”确定上下层。

## 主要改动
- 后端匹配层：
  - `collision_priority_json` 语义改为冲突组层级顺序（上→下）。
  - 取消“被长词覆盖”失败分支，冲突组内事件全部生成成功事件。
  - `MatchEvent` 增加 `layer_rank` 字段（仅冲突组赋值）。
- 后端合成层：
  - overlay 链路新增冲突组重排：低层先画，高层后画（保证上层覆盖下层）。
  - 非冲突事件保持原有流程。
- 前端文案：
  - 任务创建成功提示改为“请在冲突层级设置调整上下层”。
  - 任务详情冲突信息改为“默认层级（上→下）”，不再展示“保留/抑制”。
- 报告：
  - 新增 `冲突层级(1为最上层)` 列，便于追溯。

## 兼容性
- `POST /api/tasks` 与 `collision_priority_json` 入参保持不变。
- `POST /api/tasks/keyword-collision-check` 响应结构保持不变（继续提供默认排序依据）。

任务完成
